---
- name: correct the SSL path name in opensearch.yml
  ansible.builtin.replace:
    path: "{{ OPENSEARCH_CONF_PATH }}/opensearch.yml"
    regexp: '/opt/opensearch-2\.4\.1([^/]*?/config/SSL/)'
    replace: '/opt/opensearch-2.14.0\1'
        